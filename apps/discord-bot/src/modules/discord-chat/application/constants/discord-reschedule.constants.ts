export const RESCHEDULE_CONFIRM_CUSTOM_ID = 'reschedule_confirm';
export const RESCHEDULE_CANCEL_CUSTOM_ID = 'reschedule_cancel';

/** Pending reschedule expires after 10 minutes if user does not confirm. */
export const PENDING_RESCHEDULE_TTL_MS = 10 * 60 * 1000;
