services:
  messenger-bot:
    image: ${IMAGE:?IMAGE is required}
    container_name: messenger-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - '${PORT:-5007}:${PORT:-5007}'
